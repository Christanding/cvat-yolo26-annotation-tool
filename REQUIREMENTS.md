# 产品需求说明

状态：MVP 需求已冻结，尚未开始功能开发。

## 1. 产品定位

- 基于 CVAT Community 进行最小化二次开发。
- 面向约 10 名内部用户，但每名用户在自己的 Windows 电脑上独立安装、独立使用，不提供团队协作服务器。
- 运行形态为本地 Web 应用，通过 Docker Compose 部署。
- 支持 Windows 10/11 x64；安装完成后可离线使用，软件更新时允许联网。
- 仅供内部使用，不对外发布或商业化。
- 保留 CVAT 上游核心代码，通过配置和界面裁剪隐藏无关功能，不为追求源码体积而破坏上游升级路径。

## 2. MVP 范围

### 2.1 支持

- YOLO26 Detect 水平矩形框标注。
- 8 位 JPG、PNG 图片。
- 单任务建议规模不超过 2000 张，但不设置硬性数量上限。
- 多个本地文件夹合并为一个标注任务。
- MP4、MOV 视频的 H.264、H.265 解码与抽帧。
- 未划分的 Ultralytics YOLO Detect 标注包导入、导出。
- 本地持久化、自动保存、断点继续。

### 2.2 不包含

- 16 位图像。
- 分割、多边形、旋转框、关键点、骨架、3D、目标跟踪。
- 多用户协作、任务分配、审核流、组织和权限体系。
- 自动划分 `train`、`val`、`test`。
- MVP 阶段的模型预标注。后续直接使用 CVAT 官方自动标注能力，不创建空占位接口。
- 自动备份恢复、云存储和公网服务。
- 新品牌、Logo、配色或整体 UI 重做。

## 3. 本地工作区与数据边界

- 每台电脑配置一个固定工作根目录，由用户在首次安装时选择，不在代码中硬编码盘符。
- MVP 只引用固定工作根目录内的数据，不支持任意外部目录的动态挂载。
- 创建任务时不复制原始图片；CVAT 可在持久化目录中生成运行所需的缓存和预览。
- 任务可引用多个源文件夹，并递归读取其中的 JPG、PNG，扩展名匹配不区分大小写。
- 任务创建后，相关源文件和文件夹不得移动、改名或删除。
- 软件永不修改原始 JPG、PNG和视频。
- 本地数据库、配置和任务元数据保存在工作根目录下的 `.cvat-local/`，Docker重装或升级后必须保留。
- 用户不得手动修改 `.cvat-local/`。
- 删除任务只删除任务及标注记录，不删除原图、原视频或抽帧目录。
- 不自动清理用户数据。

## 4. 类别管理

- 用户创建任务时自行定义类别，类别名称允许中文和英文。
- 禁止空名称和重复名称。
- 开始标注前允许调整类别。
- 第一张图片开始标注后锁定类别编号和顺序。
- 标注开始后允许新增类别、修改类别名称，不允许删除类别或重新排序。
- 导出时保持锁定后的类别编号与 `data.yaml` 一致。

## 5. 图片标注流程

### 5.1 图片状态

每张图片具有以下状态之一：

- 未检查。
- 已标注：存在至少一个有效矩形框。
- 已确认无目标：不存在矩形框，但用户已确认该图片为负样本。

### 5.2 导航与完成

- `A`：上一张。
- `D`：下一张。
- 按 `D` 或点击“下一张”时，立即保存当前标注，并根据框数量将当前图片标记为“已标注”或“已确认无目标”，然后进入下一张。
- 按 `A` 或点击“上一张”时保存当前修改，但不把尚未检查的图片自动标记为完成。
- 通过缩略图、进度条或编号直接跳转时自动保存，但不自动标记完成。
- 导入已有标注包后，图片默认视为已完成；有框为“已标注”，无框为“已确认无目标”。

### 5.3 自动保存

- 创建、移动、缩放或删除矩形框后，短暂延迟自动保存。
- 图片切换前立即保存。
- 保存失败时阻止切换，并显示清楚的中文错误信息。

### 5.4 导出完整性

- 导出前检查图片与标签配对、类别编号、坐标范围和图片状态。
- 未检查图片默认阻止导出，避免将漏标图片误当作负样本。
- 已确认无目标的图片必须导出对应的空 `.txt` 标签。

## 6. 视频抽帧

### 6.1 输入与发现

- 视频必须位于固定工作根目录内。
- 用户点击“刷新目录”后发现新加入的 MP4、MOV 文件，不做实时目录监控。
- 支持设置开始时间、结束时间和抽帧间隔，单位均为整数秒。

### 6.2 输出

- 对输入视频 `<目录>/<视频名>.<扩展名>`，输出到 `<目录>/images/<视频名>/`。
- 抽取帧保存为 PNG，避免新增 JPEG 有损压缩；这不恢复输入视频编码已经损失的信息。
- 文件名格式为 `<视频名>__t000012s__f00000360.png`，同时保留整数时间戳和原视频帧号。
- 如果目标目录已经存在，提示用户选择覆盖或取消，不静默覆盖。

### 6.3 去重

- 去重范围仅限当前视频。
- 完全相同的解码帧自动跳过。
- 相似帧与“上一张已保留帧”比较，而不是与上一张原始候选帧比较。
- 提供低、中、高三档相似去重强度，默认“中”。具体阈值必须使用真实视频校准后确定。
- 相似帧自动跳过，不逐帧弹窗，不生成 `extraction_manifest.json`。
- 完成后显示：原始抽取数量、完全重复数量、相似帧跳过数量、最终保留数量。
- 可以选择另一档强度重新抽取并覆盖现有结果。
- 抽帧目录一旦用于创建标注任务，禁止覆盖重抽；必须先删除引用该目录的任务，或输出到新目录。
- 去重和重抽始终不修改原视频。

## 7. 任务导入

- 首版只导入未划分的 YOLO Detect 标注包。
- 包结构至少包含：

```text
dataset/
├── images/
├── labels/
└── data.yaml
```

- `data.yaml` 只记录类别映射，不要求 `train`、`val`、`test`。
- 图片和标签允许保留相同的嵌套子目录结构。
- 导入时校验类别、标签行格式、归一化坐标、图片标签配对和重名冲突。

## 8. 任务导出

- 导出为未划分的 YOLO Detect 标注包 ZIP。
- ZIP 包含：

```text
dataset/
├── images/
├── labels/
└── data.yaml
```

- 多个源文件夹合并时，保留来源子目录结构，避免同名图片冲突。
- `labels/` 与 `images/` 保持平行目录结构。
- 每张图片均有同名 `.txt`；确认无目标的图片生成空文件。
- `data.yaml` 只记录锁定后的类别映射。
- 不创建 `train`、`val`、`test`，因此该包是“待划分标注包”，不能直接作为正式训练配置使用。
- 导出过程只读取原图，不修改源文件。

## 9. 界面与账户

- 保留 CVAT 原有界面结构和视觉风格，进行专业、完整的中文化。
- 不制作多套方案，不新增品牌设计。
- 顶层只保留：任务列表、新建任务、图片标注、视频抽帧、导入、导出、设置。
- 隐藏组织、云存储、审核、分析、3D和自动标注等无关入口，但保留对应上游核心代码。
- 标注界面只显示矩形框、选择、缩放、平移、撤销/重做、删除和图片切换。
- 安装时自动创建一个本地账户；用户首次登录一次，之后由浏览器保持登录状态。

## 10. Windows安装、启动与更新

### 10.1 安装形式

- 采用内部离线大安装包，应用安装阶段不依赖 Docker Hub、GitHub或其他在线镜像仓库。
- Docker Desktop 是唯一需要用户预先安装的外部运行环境，使用 WSL2 后端。
- Docker Desktop 必须通过其官方安装程序安装并由用户接受许可条款；本项目安装包不捆绑或替用户静默接受 Docker Desktop 许可。
- 应用离线包至少包含安装程序、固定版本的容器镜像归档、部署配置和许可证说明。
- 用户不需要安装 Git、克隆仓库、打开终端或输入 Docker命令。

### 10.2 安装程序

用户双击 `标注软件-Setup.exe` 后，安装程序自动完成：

1. 检查 Windows版本、x64架构、WSL2、Docker Desktop、CPU虚拟化、内存、可用磁盘和目录写权限。
2. 让用户选择固定工作根目录。
3. 创建 `.cvat-local/` 持久化目录。
4. 导入固定版本的容器镜像并生成本机部署配置。
5. 启动服务并等待健康检查通过。
6. 创建本地账户和桌面快捷方式。
7. 优先使用 Microsoft Edge 打开应用；Edge不可用时回退到 Google Chrome。

### 10.3 日常使用

- 用户双击桌面快捷方式即可启动应用。
- 启动器负责检查 Docker Desktop、启动容器、等待服务就绪并打开浏览器。
- 提供停止应用入口，但停止操作不得删除容器数据、任务或源文件。
- 安装完成后，启动、标注、抽帧、导入和导出均可断网运行。

### 10.4 更新与卸载

- 更新通过新的内部安装包完成，只替换应用镜像和必要配置，并执行受控的数据迁移。
- 更新前后必须保留 `.cvat-local/`、任务、标注、原图、视频和抽帧目录。
- 卸载默认只移除应用和容器，保留 `.cvat-local/` 和全部用户数据。
- 删除持久化数据必须作为独立操作，由用户明确确认，不能附带在普通卸载中。

### 10.5 浏览器边界

- Microsoft Edge 是本项目的首选支持浏览器，必须完成真实端到端验证。
- Google Chrome 作为兼容回退浏览器。
- CVAT 上游主要支持 Chrome，因此不能仅凭二者同为 Chromium 就宣称 Edge 已兼容。

## 11. 非功能要求

- 所有核心功能在断网状态下可用。
- 真实处理约 2000 张 \(1280\times1024\) 图片时应保持可用，不以构建成功代替真实运行验证。
- 错误信息使用清楚、专业的中文，不静默失败。
- 代码修改保持最小，优先复用 CVAT 官方 API、数据模型和现有依赖。
- 不遗留临时文件、调试输出、死代码或无用文件。
- 测试与风险匹配，只验证本次功能所需的关键路径。
- SHA-256 不作为日常验证手段。

## 12. 验收主路径

MVP 至少验证以下真实流程：

1. 已安装 Docker Desktop 的 Windows 10/11 x64 电脑可以通过离线安装包完成安装，无需 Git或命令行。
2. 桌面快捷方式可启动应用并在 Edge 中打开；Chrome回退路径可用。
3. 断网后应用仍可启动和完成核心工作流。
4. 固定工作区新增视频后，手动刷新可以发现并抽帧。
5. H.264/H.265 的 MP4、MOV按指定时间范围和整数秒间隔输出 PNG。
6. 三档相似去重输出正确统计，原视频保持不变。
7. 从多个文件夹递归创建一个任务，并处理同名图片。
8. 矩形框操作、`A`/`D` 导航、图片状态和自动保存正常。
9. 应用与 Docker重启、更新后任务、类别和标注仍存在。
10. 未检查图片不能直接导出；负样本生成空标签。
11. 导出包保留嵌套目录，并可重新导入且保持图片数、类别和标注一致。
12. 删除任务或卸载应用不删除任何源图片、视频、抽帧目录或默认持久化数据。

## 13. 实现前仍需确定

- 固定使用的 CVAT Community 稳定版本及其依赖、许可证和升级说明。
- `标注软件-Setup.exe` 的具体构建技术及其许可证。
- 低、中、高三档相似帧去重的具体算法和阈值。
- H.264/MOV兼容性测试素材；当前真实样本主要覆盖 HEVC MP4。
- 正式内部发布前，由负责人确认组织使用 Docker Desktop 所需的许可。
